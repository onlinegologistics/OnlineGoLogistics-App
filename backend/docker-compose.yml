version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: onlinego-backend
    restart: always
    ports:
      - "${PORT:-5001}:${PORT:-5001}"
    env_file:
      - .env
